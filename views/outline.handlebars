
<div class="container">
 <br>
 <div class="jumbotron">
 <br>
 <div id="projects messages">

    <h4> {{project.name}} </h4>
    <p>  {{project.description}} </p>
    
    {{#each project.tags as |tag|}}
      <h4>{{message.tag.tag_name}}</h4>
   {{/each}}

</div>
</div>